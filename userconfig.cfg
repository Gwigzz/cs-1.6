echo 
echo 
echo *************************************
echo __ userconfig.cfg cs 1.6 is running _
echo ----------- 2003 - 2024 -------------
echo ------------ By Gwigzz --------------
echo *************************************
echo  

// options de lancement à mettre dans steam : -console -gl -nofbo -width 800 -noforcemaccel
// forcer les résolutions :
// -w 640 -h 480
// -w 800 -h 600
// -w 1024 -h 768

// ___ deffault ___
// fps_override                0
// fps_max                     100

// ___ unblock fps but can bugs happend ___
fps_override                1
fps_max                     165

cl_crosshair_color          "999 999 999"
cl_dynamiccrosshair         0
cl_updaterate               101
cl_cmdrate                  101
_cl_autowepswitch           0

// ignore models player (skin, knife ...)
mp_consistency              0

ex_interp                   0.01
rate                        25000
m_filter                    1

lightgamma                  0
// gamma                       5

// brightness
brightness                  999
gamma                       999

// podbot ADMIN
bind "p" "amx_pbmenu"
// amxmodmenu ADMIN
bind "," "amxmodmenu"
echo AMX local-server only, type:
echo [(p) podbot menu]
echo [(,) amxmodmenu]

echo 
echo ###########################################


// alias "w10" "wait; wait; wait; wait; wait; wait; wait; wait; wait; wait"
// alias "cros" "cl_lw 1; cl_dynamiccrosshair 0; +duck; w10; w10; w10; w10; w10; cl_lw 0; -duck"

bind j "say_team RUSH B SUKA BLYAT !!!"